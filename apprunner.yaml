version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm i
      - npm i ts-node-dev --save-dev
      - npm install -D tslib @types/node
      - npm run build
      - npx prisma generate
run:
  command: ts-node dist/QBDbin/run.js
  network:
    port: 8000
  env:
    - name: DATABASE_URL
      value: postgresql://postgres:Satva1213@sage-intacct.ci6m9hlbprkn.us-east-1.rds.amazonaws.com:5432/reusable-app?schema=public
    - name: QB_SOAP_PORT
      value: 8000
